name: Security Scan

on:
  push:
    branches:
      - main

jobs:
  security_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          java-version: '11'

      - name: Install ZAP
        run: |
          sudo apt-get update
          sudo apt-get install -y zaproxy

      - name: Run ZAP Security Test
        run: |
          chmod +x ./zap_test.sh
          ./zap_test.sh

      - name: Upload Report
        uses: actions/upload-artifact@v2
        with:
          name: zap-report
          path: /path/to/report.html
